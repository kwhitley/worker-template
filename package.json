{
  "private": true,
  "name": "worker-template",
  "version": "0.1.0",
  "description": "Worker API Template",
  "module": "./dist/index.mjs",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "lint": "eslint src",
    "build": "esbuild src/index.js --format=esm --bundle --outfile=dist/index.mjs",
    "release": "release --push --nopublish --patch",
    "postrelease": "wrangler publish"
  },
  "author": "Kevin Whitley <krwhitley@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.15.10",
    "eslint": "^8.24.0",
    "eslint-plugin-jest": "^27.0.4",
    "prettier": "^2.7.1"
  },
  "dependencies": {
    "cloudflare-kv-store": "^0.5.2",
    "itty-cors": "^0.1.0",
    "itty-durable": "^1.2.0",
    "itty-router": "^2.6.6",
    "itty-router-extras": "^0.4.2"
  }
}
